# Interop Library
The interop library is meant to provide an abstraction layer between a resource type and their implementation. 
For example, we might want to load a *configuration* file for a program currently in the XML format, however, we don't want to box the code into using a specific XML library.
